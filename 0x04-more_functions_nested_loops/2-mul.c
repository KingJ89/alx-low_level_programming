#include "main.h"

/**
 * mul - prints the product of two integers
 * @x: the first number to multiply
 * @y: the second number to multiply
 * Return: the result of multiplication param x and param y
 */

int mul(int x, int y)
{
	return (x*y);
}
