#include <stdio.h>

/**
 * main - Main  Entry point
 *
 * Return: return always 0
 */

int main(void)
{

	char jm;

	{
	for (jm = 'z' ; jm >= 'a' ; jm--)
		putchar(jm);
	putchar('\n');
	}
	return (0);
}
