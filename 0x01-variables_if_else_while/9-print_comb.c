#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: return 0
 */

int main(void)
{
	int jm;

	for (jm = 0 ; jm < 10 ; jm++)
	{
		putchar(jm);
		putchar(',');
		putchar(' ');
	}
		return (0);
}
